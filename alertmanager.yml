global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'jenkins-rollback-webhook'

receivers:
  - name: 'jenkins-rollback-webhook'
    webhook_configs:
      - url: 'http://3.222.182.127:8080/generic-webhook-trigger/invoke?token=ROLLBACK_9C63D3_TOKEN'
        send_resolved: false
        http_config:
          basic_auth:
            username: 'admin'
            password: '11c65634e8bc734bb23676c11fdb52b366'
