global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'jenkins-rollback-webhook'

receivers:
  - name: 'jenkins-rollback-webhook'
    webhook_configs:
      - url: 'http://44.199.50.27:8080/generic-webhook-trigger/invoke?token=ROLLBACK_0684BD_TOKEN'
        send_resolved: false
        http_config:
          basic_auth:
            username: 'root'
            password: '115dd5fb0f899194276528c76dd5f474d6'
